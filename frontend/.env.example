# Optional absolute API origin (e.g. http://127.0.0.1:8000). Leave unset for same-origin and Vite /api proxy.
# VITE_API_BASE_URL=

# Optional override for Vite dev server proxy target (default http://127.0.0.1:8000).
# VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8000
