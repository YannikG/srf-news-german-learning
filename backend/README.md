# Backend

Python service area. The `Dockerfile` here builds the `web` image for Compose (placeholder HTTP server until the Flask app factory ships in P1-I02).

Local virtualenv and dependencies will be documented in P1-I02 alongside `requirements.txt`.
